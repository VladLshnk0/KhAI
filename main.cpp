#include <QApplication>
#include <QPushButton>
#include <QLabel>
 
int main(int argc, char *argv[ ]) 
{
QApplication app(argc, argv);
 
Qlabel = label = new QLabel
label.show( );
 
return app.exec( );
}